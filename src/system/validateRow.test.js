import validateRow from './validateRow.js';
import Focus from '../utils/Focus.js';
import {ADMIN_ACCOUNT} from '../SETTINGS.js';

// =====================================================================================================================
//  D E C L A R A T I O N S
// =====================================================================================================================
const ROW = {
    from: 'Alice',
    value: 7,
    to: 'Bob',
    product: 'Lorem Ipsum',
    date: '2024-10-10T10:10:00+03:00',
};
// =====================================================================================================================
//  T E S T S
// =====================================================================================================================
const tests = [
    // -----------------------------------------------------------------------------------------------------------------
    {
        importance: 1,
        input: {...ROW},
        output: true,
    },
    // -----------------------------------------------------------------------------------------------------------------
    {
        importance: 1,
        input: {...ROW, from: 5},
        output: 'REGEX:.',
    },
    // -----------------------------------------------------------------------------------------------------------------
    {
        importance: 1,
        input: {...ROW, from: ''},
        output: 'REGEX:.',
    },
    // -----------------------------------------------------------------------------------------------------------------
    {
        importance: 1,
        input: {...ROW, from: 'Alice '},
        output: 'REGEX:.',
    },
    // -----------------------------------------------------------------------------------------------------------------
    {
        importance: 1,
        input: {...ROW, value: 'foo'},
        output: 'REGEX:.',
    },
    // -----------------------------------------------------------------------------------------------------------------
    {
        importance: 1,
        input: {...ROW, value: 0, from: 'foo'},
        output: 'REGEX:.',
    },
    // -----------------------------------------------------------------------------------------------------------------
    {
        importance: 1,
        input: {...ROW, value: 0, from: ADMIN_ACCOUNT},
        output: true,
    },
    // -----------------------------------------------------------------------------------------------------------------
    {
        importance: 1,
        input: {...ROW, value: -1},
        output: 'REGEX:.',
    },
    // -----------------------------------------------------------------------------------------------------------------
    {
        importance: 1,
        input: {...ROW, to: 7},
        output: 'REGEX:.',
    },
    // -----------------------------------------------------------------------------------------------------------------
    {
        importance: 1,
        input: {...ROW, to: ''},
        output: 'REGEX:.',
    },
    // -----------------------------------------------------------------------------------------------------------------
    {
        importance: 1,
        input: {...ROW, to: 'Bob '},
        output: 'REGEX:.',
    },
    // -----------------------------------------------------------------------------------------------------------------
    {
        importance: 1,
        input: {...ROW, to: ADMIN_ACCOUNT},
        output: 'REGEX:.',
    },
    // -----------------------------------------------------------------------------------------------------------------
    {
        importance: 1,
        input: {...ROW, to: ROW.from},
        output: 'REGEX:.',
    },
    // -----------------------------------------------------------------------------------------------------------------
    {
        importance: 1,
        input: {...ROW, product: 7},
        output: 'REGEX:.',
    },
    // -----------------------------------------------------------------------------------------------------------------
    {
        importance: 1,
        input: {...ROW, product: ''},
        output: 'REGEX:.',
    },
    // -----------------------------------------------------------------------------------------------------------------
    {
        importance: 1,
        input: {...ROW, product: 'Hello World'},
        output: true,
    },
    // -----------------------------------------------------------------------------------------------------------------
    {
        importance: 1,
        input: {...ROW, product: 'Hello\tWorld'},
        output: 'REGEX:.',
    },
    // -----------------------------------------------------------------------------------------------------------------
    {
        importance: 1,
        input: {...ROW, product: ' foo'},
        output: 'REGEX:.',
    },
    // -----------------------------------------------------------------------------------------------------------------
    {
        importance: 1,
        input: {...ROW, product: 'foo  bar'},
        output: 'REGEX:.',
    },
    // -----------------------------------------------------------------------------------------------------------------
    {
        importance: 1,
        input: {...ROW, date: '2023-11-02T05:57:28.961Z'},
        output: 'REGEX:.',
    },
];

// =====================================================================================================================
//  R U N
// =====================================================================================================================
Focus.run(validateRow, tests, {
    // sendInputAsArguments: true
});
